# NPM Configuration for E-Cell Website
# Suppress deprecation warnings for packages we can't control
audit-level=moderate
fund=false
legacy-peer-deps=false

# Performance optimizations
prefer-offline=true
progress=true

# Security settings
package-lock=true
save-exact=false

# Suppress specific deprecated package warnings
# These are transitive dependencies we cannot directly control
loglevel=warn
